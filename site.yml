---
- name: Set-up the entire Zeus infrastructure
  hosts: king
  user: root

  roles:
    - common
    - users
    - mysql
    - nginx
    - php
    - passenger
    - ruby_and_rails
    - python
    # Install nginx configs after fcgi and passenger are installed
    - nginx_configs
